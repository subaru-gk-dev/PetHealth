---
name: daily-2026-09-11
type: project
---

# 2026-09-11

## 実機で触れる状態まで（ユーザー指示「確認は不要なので実機確認できるところまで進めて」）

- **Artifact 公開**＝<https://claude.ai/code/artifact/4fcea45f-2c14-4892-896c-732685877a5f>（claude.ai 上の非公開ページ・Android Chrome で開いて試せる）。中身は `npm run build:artifact` の 1 枚 HTML（`scripts/build-artifact.mjs`＝`vite-plugin-singlefile` で束ねて `<title>/<style>/<div id="app">/<script>` だけに整形）。
  - できる＝カメラ起動・便/飲水/食事/尿/薬/メモの記録・タイムライン・グラフ・端末内保存。
  - できない＝ホーム画面追加（SW なし）・家族共有（Firebase は CSP で通らない）・JSON 書き出し（ダウンロード権限なし → 試用版では非表示にして案内文を出す `VITE_SINGLE_FILE`）。
- **Firebase を遅延読込**に分離＝`config.ts`（env だけ）／`firebase.ts`／`store/cloudSession.ts`（ログイン・世帯）を `session.ts` から `import()`。初回読込 862 kB → 215 kB。`main.tsx` の SW 登録も動的 import＋`VITE_NO_SW` で無効化可。
- **写真は data: URL** で表示（`util/blob.ts`）。Artifact の CSP で `blob:` が塞がれても出るように。
- **ダーク対応**＝`styles.css` にトークンの再定義（`prefers-color-scheme` ＋ `data-theme`）。Chart.js の文字・線の色は `getComputedStyle` でトークンから取る（`Charts.tsx`）。
- **GitHub Pages の配信ワークフロー**＝`.github/workflows/pages.yml`（main への push で build → Pages。`VITE_BASE=/PetHealth/`。Firebase の値は Actions の Variables から）。`vite.config.ts` の `base`／manifest の `start_url`/`scope` は `VITE_BASE` で切替。
- 検証＝tsc 0件・Vitest 8本緑・PWA 版と 1 枚 HTML 版の両方で Chromium スモーク（ペット登録→便に写真→保存→タイムライン→飲水→再読込→グラフ）OK。
- ⚠️**GitHub の新規リポジトリ作成は 403**（`POST /user/repos`、2回試行）。`subaru-gk-dev/PetHealth` はユーザーが作る必要あり。作成後＝このリポジトリ（`/home/user/PetHealth` main）を push → Settings → Pages → Source を GitHub Actions に。
- 踏んだ罠＝`pkill -f "vite preview"` は自分のシェルも殺す（exit 144）。`ps -eo pid,args | grep "[v]ite.js preview"` で pid を取ってから kill。

## GitHub リポジトリ作成後（ユーザーが `subaru-gk-dev/PetHealth`・Public で作成）
- `git push -u origin main` 完了（3 コミット）。セッションの clone 先は `/home/user/pethealth`（小文字）＝`/home/user/PetHealth` へのシンボリックリンク。
- Pages ワークフロー初回＝build 成功・deploy 失敗（Pages 未有効）。`actions/configure-pages@v5` を `enablement: true` で build ジョブに足して再 push。
- ユーザーが Settings → Pages → Source＝GitHub Actions に設定。Actions の dispatch/rerun は 403 なので README を直す commit を push して再実行 → **deploy 成功**。公開 URL＝<https://subaru-gk-dev.github.io/PetHealth/>（PWA・端末内モード。家族共有は Firebase の値を Actions の Variables に入れてから）。サンドボックスからは github.io に到達できないため、画面の実確認はユーザーの実機で。
